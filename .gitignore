.env
.tmp/

# Dolt database files (added by bd init)
.dolt/
*.db

# Agent identity marker (per-worktree, not committed)
.bd-agent-identity
.workflow/issue-tracker.md

# Claude Code worktrees (local agent workspaces)
.claude/
